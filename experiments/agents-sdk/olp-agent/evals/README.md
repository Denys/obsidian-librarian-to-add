# Local Evals

`run_local.py` executes deterministic cases against the real app functions. These evals do not require `OPENAI_API_KEY`; live model evals can be added as a separate lane once credentials and model access are available.

Run:

```powershell
.\.venv\Scripts\python.exe evals\run_local.py
```
